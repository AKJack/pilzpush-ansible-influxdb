---
# defaults file for pilzpush-ansible-influxdb
pilzpush_database: {}
pilzpush_database_defaults:
  config_dir: /etc/influxdb2
  data_dir: /var/lib/influxdb2
  ports:
   - "8086:8086"
  version: latest
